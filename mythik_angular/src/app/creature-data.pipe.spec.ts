import { CreatureDataPipe } from './creature-data.pipe';

describe('CreatureDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatureDataPipe();
    expect(pipe).toBeTruthy();
  });
});
