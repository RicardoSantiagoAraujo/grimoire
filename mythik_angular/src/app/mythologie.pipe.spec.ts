import { MythologiePipe } from './mythologie.pipe';

describe('MythologiePipe', () => {
  it('create an instance', () => {
    const pipe = new MythologiePipe();
    expect(pipe).toBeTruthy();
  });
});
