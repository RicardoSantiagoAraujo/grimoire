import { ReduceImagePipe } from './reduce-image.pipe';

describe('ReduceImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceImagePipe();
    expect(pipe).toBeTruthy();
  });
});
